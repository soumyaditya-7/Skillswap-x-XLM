[package]
name = "skill-swap-v2"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "21" }

[dev-dependencies]
soroban-sdk = { version = "21", features = ["testutils"] }
